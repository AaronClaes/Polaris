node_modules
dist
out
.DS_Store
.eslintcache
*.log*

# Local environment (OAuth client credentials, etc.) — keep the example tracked.
.env
.env.*
!.env.example

# TypeScript incremental build info
*.tsbuildinfo
